hydra:
  searchpath:
    - file://verl/trainer/config
    - file://recipe/phimm/config
    - file://recipe/phimm/config/base
defaults:
  - long_eval_asr
  - data/val_data/inhouse_2605_engb_seg
  - _self_

# Pre-segmented variant of long_eval_inhouse_2605_engb. The audio is chunked
# OFFLINE by recipe.phimm.data.long_audio_vad_explode (same SVAD logic), so the
# val_data default carries no svad_explode pre_process. Base knobs live in
# base/long_eval_asr.yaml; only set per-eval fields here.
# Results: {output_path}/result_details.jsonl + {output_path}/measures.json

actor_rollout_ref:
  model:
    path: az://orngwus2cresco/data/speech/projects/phi-fastllm-2605/amlt-results/fast-llm-2605-qwen3-5-9b-s2-st-example-r2/90000/qwen_hf/

data:
  output_path: az://orngwus2cresco/data/boren/data/verl/eval/qwen_r2_baseline/inhouse_2605_engb_seg
