hydra:
  searchpath:
    - file://recipe/phimm/config/base
    - file://verl/trainer/config
defaults:
  - dapo_asr
  - data/train_data/ls_raw # not used
  - data/val_data/bias_entity_ls
  - _self_

actor_rollout_ref:
  rollout:
    n: 8
  model:
    path: ${oc.env:DATA_PATH}/boren/outputs/grpo_ls_m1000_seed_e2_simple_err_t12_n8_bp8_imp5_ref_sc1k_char_bias5_smp2a_G2x8/checkpoint-4380
data:
  max_prompt_length: 8192 # requred by 7b model

trainer:
  val_only: True
  nnodes: 1
