dataset_name: tsv
cache_name: auto_train
tsv_paths: ${oc.env:DATA_PATH}/boren/data/LibriSpeech/h1k.tsv
add_prompt:
  task: asr
post_process:
  add_field:
    fields:
      data_source: asr
  verl_format:
    prompt_key: prompt
